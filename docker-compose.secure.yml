# З BOLA/: docker compose -f docker-compose.yml -f docker-compose.secure.yml up --build
services:
  reports:
    environment:
      REPORTS_AUTHZ_MODE: secure
  gateway:
    environment:
      REPORTS_AUTHZ_MODE: secure
